import React from "https://esm.sh/react";
export default function Input(_a) {
    var type = _a.type, placeholder = _a.placeholder;
    return React.createElement("input", { className: "input", type: type, placeholder: placeholder });
}
